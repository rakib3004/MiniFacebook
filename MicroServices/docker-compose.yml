version: "3"

services:
    nginx:
        image: nginx:latest
        restart: unless-stopped
        ports:
            - 100:100
        volumes:
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
        depends_on:
            - userservice
            - statusservice
            - storyservice

    userservice:
        build: ./UserService
        restart: unless-stopped
        depends_on:
            - userdb

    userdb:
        image: mongo:4.0-xenial
        ports: 
            - 27018:27017
        restart: unless-stopped
        volumes:
            - ./mongoUser:/data/db
    statusservice:
        build: ./StatusService
        restart: unless-stopped
        depends_on:
            - statusdb

    statusdb:
        image: mongo:4.0-xenial
        ports: 
            - 27019:27017
        restart: unless-stopped
        volumes:
            - ./mongoStatus:/data/db

    storyservice:
        build: ./StoryService
        restart: unless-stopped
        depends_on:
            - storydb
            - storyobjectdb

    storydb:
        image: mongo:4.0-xenial
        ports: 
            - 27020:27017
        restart: unless-stopped
        volumes:
            - ./mongoStory:/data/db

    storyobjectdb:
        image: minio/minio:latest
        environment:
            - MINIO_ACCESS_KEY=W39XDvxvVcYYOIpz
            - MINIO_SECRET_KEY=HtyHG1IKzWhjeuGdqIzasFOnv0pK0vpT
        command: server --address 0.0.0.0:9000 /data

volumes:
    mongoUser:
    mongoStatus:
    mongoStory:
